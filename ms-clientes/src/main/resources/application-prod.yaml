spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${MS_CLIENTES_DB_URL}
    username: ${MS_CLIENTES_DB_USERNAME}
    password: ${MS_CLIENTES_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}
